_schema-version: "3.2"
ID: invoicedemoo
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: invoicedemoo-destination-content
  type: com.sap.application.content
  requires:
  - name: invoicedemoo-destination-service
    parameters:
      content-target: true
  - name: invoicedemoo-repo-host
    parameters:
      service-key:
        name: invoicedemoo-repo-host-key
  - name: invoicedemoo-uaa
    parameters:
      service-key:
        name: invoicedemoo-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: invoicedemoo_html_repo_host
          ServiceInstanceName: invoicedemoo-html5-srv
          ServiceKeyName: invoicedemoo-repo-host-key
          sap.cloud.service: invoicedemoo
        - Authentication: OAuth2UserTokenExchange
          Name: invoicedemoo_uaa
          ServiceInstanceName: invoicedemoo-xsuaa-srv
          ServiceKeyName: invoicedemoo-uaa-key
          sap.cloud.service: invoicedemoo
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: invoicedemoo-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: invoicedemoo-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - invoicedemoo.zip
      name: invoicedemoo
      target-path: resources/
- name: invoicedemoo
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: invoicedemoo-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: invoicedemoo-destination-service
    service-plan: lite
- name: invoicedemoo-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: invoicedemoo-xsuaa-srv
    service-plan: application
- name: invoicedemoo-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: invoicedemoo-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
